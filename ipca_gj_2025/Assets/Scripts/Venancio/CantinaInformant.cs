using UnityEngine;

public class CantinaInformant : MonoBehaviour
{
    [Header("References")]
	[SerializeField] private GameObject _informantGameObject;
    [SerializeField] private string _informantName;
    [SerializeField] private Animator _animator;

	// Start is called once before the first execution of Update after the MonoBehaviour is created
	void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
